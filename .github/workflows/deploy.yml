name: Deploy to AWS Lambda

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Install Dependencies
        run: npm install

      - name: Verify Node Modules
        run: ls -l node_modules

      - name: Zip for Lambda
        run: zip -r /home/runner/work/aquamark-lambda/aquamark-lambda.zip index.mjs node_modules package.json

      - name: Verify Zip Exists
        run: ls -l /home/runner/work/aquamark-lambda/aquamark-lambda.zip

      - name: Update Lambda Function with Debug
        env:
          AWS_REGION: us-east-1
        run: |
          echo "Updating Lambda Function..."
          aws lambda update-function-code \
            --function-name AquamarkHandler \
            --zip-file fileb:///home/runner/work/aquamark-lambda/aquamark-lambda.zip \
            --publish \
            --debug
